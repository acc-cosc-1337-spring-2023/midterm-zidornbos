//Write a function to show how value and reference parameters work.  
//Write the relevant test cases(must have at least two REQUIRE assertions for the function) and main.cpp code.

bool test_config();

int val_param(int p1);
int ref_param(int &p1);

